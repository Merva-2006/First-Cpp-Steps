#include<iostream>
using namespace std;
int main(){
    float x;
    cout<<"Enter your grade \n";
    cin>>x;
    if(x<=100 && x>=90)
    cout<<"Excellent \n";
    else if(x<90 && x>=80 )
    cout<<"Very Good \n";
    else if(x<80 && x>=70)
    cout<<"Good \n";
    else if(x<70 && x>=60)
    cout<<"Acceptable \n";
    else if( x<60 && x>=0)
    cout<<"Weak \n";
    else 
    cout<<" try again \n";
 return 0;
}
